<x-layouts.admin title="Resumen">
    <div class="admin-heading"><div><span>Panel editorial</span><h1>Hola, {{ str(auth()->user()->name)->before(' ') }}.</h1><p>Aquí tienes una vista rápida de lo que está ocurriendo en Open Lab.</p></div><div class="flex flex-wrap gap-2"><x-button label="Editar sitio" icon="o-paint-brush" :link="route('admin.site.edit')" /><x-button label="Nueva publicación" icon="o-plus" :link="route('admin.posts.create')" class="openlab-primary" /></div></div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 my-8"><x-stat title="Publicaciones" :value="$postsCount" icon="o-newspaper" class="bg-white border border-black/10 shadow-none" /><x-stat title="Publicadas" :value="$publishedCount" icon="o-check-circle" class="bg-white border border-black/10 shadow-none" /><x-stat title="Consultas nuevas" :value="$inquiriesCount" icon="o-inbox" class="bg-[#161616] text-white shadow-none" /></div>
    <div class="bg-white border border-black/10 p-6"><div class="flex items-center justify-between mb-6"><h2 class="font-display text-xl font-semibold">Publicaciones recientes</h2><a href="{{ route('admin.posts.index') }}" class="text-sm underline">Ver todas</a></div><div class="divide-y divide-black/10">@forelse($recentPosts as $post)<div class="py-4 flex items-center justify-between gap-4"><div><span class="text-xs uppercase tracking-wider text-black/40">{{ $post->category }}</span><p class="font-medium">{{ $post->title }}</p></div><div class="flex items-center gap-3"><x-badge :value="$post->status === 'published' ? 'Publicada' : 'Borrador'" :class="$post->status === 'published' ? 'badge-success badge-soft' : 'badge-ghost'" /><x-button icon="o-pencil" :link="route('admin.posts.edit', $post)" class="btn-sm btn-ghost" /></div></div>@empty<p class="py-8 text-black/50">Aún no hay publicaciones. Crea la primera para comenzar.</p>@endforelse</div></div>
</x-layouts.admin>
