<div><x-header title="Consultas" subtitle="Requerimientos enviados desde el formulario de contacto." separator />
<div class="space-y-3">@forelse($inquiries as $inquiry)<x-collapse :open="!$inquiry->read_at" class="bg-white border border-black/10"><x-slot:heading><div class="flex flex-wrap items-center gap-3"><span class="font-medium">{{ $inquiry->name }}</span>@if(!$inquiry->read_at)<x-badge value="Nueva" class="badge-warning badge-soft" />@endif<span class="text-sm text-black/45">{{ $inquiry->organization }} · {{ $inquiry->created_at->diffForHumans() }}</span></div></x-slot:heading><x-slot:content><div class="grid md:grid-cols-[180px_1fr] gap-5 pt-2"><div class="text-sm space-y-2"><p><strong>Rol</strong><br>{{ $inquiry->role }}</p><p><strong>Área</strong><br>{{ $inquiry->area }}</p><a href="mailto:{{ $inquiry->email }}" class="underline">{{ $inquiry->email }}</a></div><div><p class="leading-relaxed">{{ $inquiry->message }}</p><div class="mt-4 flex gap-2"><x-button label="Responder por correo" icon="o-envelope" link="mailto:{{ $inquiry->email }}" class="btn-sm btn-primary" />@if(!$inquiry->read_at)<x-button label="Marcar como leída" icon="o-check" wire:click="markRead({{ $inquiry->id }})" class="btn-sm btn-ghost" />@endif</div></div></div></x-slot:content></x-collapse>@empty<div class="bg-white p-12 text-center text-black/50">Todavía no hay consultas.</div>@endforelse</div><div class="mt-5">{{ $inquiries->links() }}</div></div>
